# AppTracker Frontend

This is a minimal React + Vite frontend skeleton.

Run:

```powershell
cd C:\Users\abhih\AppTracker\frontend
npm install
npm run dev
```

Environment:

- `VITE_API_URL` - set to your backend base url (e.g. http://localhost:8080)
